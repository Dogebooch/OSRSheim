# OSRSheim territories — TEMPLATE ONLY (needs Gielheim coordinates; nothing here is live)
# Docs: https://kg-marketplace.pages.dev/configs/territories/
# Block layout: [ZoneName@priority] / Shape (Circle|Square|Rectangle) / x, y, radius (Circle) /
#   r, g, b colour / flags (comma separated) / access (None)
# Ideas once the world exists (fill coordinates from the in-game "pos" command):
#   SpawnTown  — the spawn town: NoMonsters, NoBuildDamage, RevealOnMap
#   Wilderness — a PvP ring far north: PvpOnly, DropMultiplier 2, IncreasedPlayerDamage
# Example (commented out on purpose; territory names are case-sensitive):
#[SpawnTown@1]
#Circle
#0, 0, 60
#120, 200, 120
#NoMonsters, NoBuildDamage, RevealOnMap
#None
