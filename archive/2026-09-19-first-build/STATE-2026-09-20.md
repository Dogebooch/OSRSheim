# STATE — last updated 2026-09-20 (morning test run; Blacksmithing/Ranching bumped)

**Status.** Stack loads clean on Valheim 1.0.15 with **Blacksmithing 1.3.6 +
Ranching 1.1.9** (installed via Gale between the 07:56 and 08:00 launches).
The 08:00 launch: 0 exceptions in `LogOutput.log` / `Player.log`, WIRSL
`Loaded: 217`, Spawn That world spawners 500-511 loaded, Drop That `.104/.105`
superior blocks loaded, ModTest world entered. Validator: 0 errors / 0 warnings.

**In-game results live in `TEST-RUN-2026-09-20.md`** (30-row table, one line per
test). Passed: 1 coins stack, 2 clone drop off a kill (p9-9), 3a boss purse.
Open: 3b hiscore, 4-30. The `raiseskill` blocker (Blacksmithing 1.3.5 called
the pre-1.0 4-arg `Character.Message`) is fixed by 1.3.6 but `raiseskill` has
not been re-run since; tests 9/11/12 depend on it.

**Next session:** `python scripts\validate-configs.py` -> launch ModTest ->
re-check `raiseskill Swords 15` first -> continue the TEST-RUN table from 3b.
`[WorldSpawner.500]` is still at its TEMP test values (SpawnChance 100 /
SpawnInterval 10, commented in the file) for test 6; restore 8 / 900 after.

## Stack — 24 active mods, pinned in Gale, all loading with zero errors
- Core: BepInEx 5.4.2350 · Jötunn 2.30.1
- Skills (Smoothbrain ×10): Mining 1.1.7, Lumberjacking 1.0.7, Cooking 1.2.3,
  Farming 2.2.3, **Blacksmithing 1.3.6**, Building 1.2.7, Sailing 1.1.9,
  **Ranching 1.1.9**, Foraging 1.0.11, Evasion 1.0.5 — XP gain 0.5x, death XP
  loss 0 (Evasion's keys are "Skill gain factor" / "Skill loss")
- Gating: WackyItemRequiresSkillLevel 1.4.7 · WackysDatabase 2.5.31
- Loot: Drop That 3.1.5 · Spawn That 1.2.19 · CLLC 5.0.3 (Maximum stars None,
  Loot system Vanilla, item yaml ON) · EpicLoot 0.14.10 (magic drops zeroed;
  Adventure Mode ON, coins-only)
- Economy: KG Marketplace 10.0.1-beta.1 (Use Marketplace Locally = true until
  the server exists; Doug's SteamID64 already in `OverrideDebug`)
- Inventory/travel: AzuExtendedPlayerInventory 2.5.1 · XPortal 1.2.25
- World: More World Locations AIO 5.1.1 (Trainers OFF, custom trader YAML ON)
- DISABLED, keep installed: Almanac 3.7.94 (pre-1.0 ServerSync crash; 66
  achievement files staged and parsing). REMOVED: Valheim1CompatBridges.

## Config files (all under `<profile>\BepInEx\config\`; rollbacks in HANDOFF §1)
- `WackyMole.ItemRequiresSkillLevel.yml` — 217 gates (bronze 15 / iron 20-25 /
  silver+wolf 30 / blackmetal+padded 40 / Mistlands 50 / Ashlands 60 / Deep
  North 70; staves on ElementalMagic/BloodMagic; equip on Blocking, craft on
  Blacksmithing; 24 skillcapes at 100). A wrong PrefabName fails OPEN silently.
  Deliberately ungated: Shovel, HelmetLox, HelmetCrownofValheim, vanilla capes,
  SledgeStagbreaker, BowFineWood, ArrowFlint. Backup `.bak-tier607080`.
- `drop_that.character_drop.cfg` — full bestiary (commons coins + Gem tier,
  elites coins + Rare tier, bosses flat coins, p9-9 uniques `.102` 2.94% / pets
  `.103` 0.02%). Boss purses split into <=100 chunks (`.100` + `.106+`);
  `DropOnePerPlayer` removed from the 8 boss purses (`.bak-dropone`), still on
  the 16 unique/pet entries where amount is 1 anyway.
- `drop_that.character_drop.osrsheim_superiors.cfg` — `.104/.105` on
  Greydwarf/Skeleton/Draugr/Wolf/Goblin/Seeker/Charred_Melee/JotunWarrior gated
  `ConditionMinLevel = 3`; Troll gated `ConditionBiomes = Meadows`. Untested.
- `drop_that.character_drop_list.shared_tables.cfg` — Gem tiers 1-4, Rare
  tiers 2-5 at list IDs 110+/120+, >100 chunks at `.130+`. One UseDropList
  per creature.
- `drop_that.drop_table.cfg` — tertiaries: Feathers on 12 logs, gems on ore
  deposits, `TreasureChest_forestcrypt`.
- `drop_that.cfg` — dump flags ON, `AlwaysAutoStack = true`.
  `spawn_that.cfg` — dump flags ON.
- `spawn_that.world_spawners_advanced.cfg` — 500-507 Superiors (two-star,
  CLLC extra effect, 8% / 900 s / cap 1, boss-key gated), 510 Meadows night
  troll, 511 Black Forest fuling scouts. Untested. `.bak-empty`.
- `wackysDatabase\Items\Item_OSRS_*.yml` — 40 clones (8 uniques, 8 pets, 24
  capes). wackydb only reads `Item_*.yml`; never `wackydb_save_item` onto an
  authored file. Base dumps: `reference\wackydb-base-dumps\`.
- `EpicLoot\baseconfig\loottables.json` (all `[[0,100]]`) and
  `adventuredata.json` (coins-only) — `.bak-osrsheim`, notes in
  `EpicLoot\OSRSheim-NOTES.md`.
- `Marketplace\Configs\**\osrsheim_*.cfg` + `RandomNpcSpeech.yml` — the KG
  content pack (3 traders, bank, 2 gamblers, 13 story quests, 14 Slayer tasks,
  dialogues, 5 prayers, 23 hiscores, info page). Parsed with zero per-entry
  errors; NPCs not yet placed (tests 13-26).
- `ItemConfig.yml` — Coins weight 0 / stack 9999 (verified in game), gems light.
- `warpalicious.More_World_Locations_TraderItems.yml` — generated, then
  stripped of Skill Books / Blacksmith Stones (`.bak-generated`). DONE.
- `Almanac\Achievements\OSRSheim *.yml` — staged for the 1.0 build.
- `drop_that.character_drop.literal.cfg.disabled` — literal-fraction swap.

## Drop That facts learned in game 2026-09-20 (load-bearing)
- Each drop ENTRY caps at 100 items per kill (150 paid 100; two 80s paid 160).
  Anything above 100 must be split across entries.
- `DropOnePerPlayer = true` forces the amount to 1. Never use it on a purse.
  Whether it also rolls once per player on a server is still unverified (16).
- Console `spawn <Prefab> <n> 3` = two stars; CLLC may clamp it (test 5).

## Open items (numbers preserved; done items removed)
4. Re-enable Almanac when a 1.0 build ships (Thunderstore still 3.7.94); the
   22 collection-log entries are already staged.
5. Watch KG Marketplace for a stable 10.x (still 10.0.1-beta.1).
8. Phase 8 server standup: Gale profile export -> friend; delete ModTest; stand
   up Gielheim. Flags verified in `scripts\server-start-template.bat`
   (`-modifier deathpenalty casual` recommended, `portals` undecided,
   `-setkey playerevents` optional). SteamID64 into `adminlist.txt`.
9. Goblin double coins (vanilla 25% x 5-10 + ours 30% x 5-15) — tune if rich.
   Fishing bait names still need a live wackydb dump (with 22).
10. Wave B mods, one at a time: JuJuz1/SkillGainModifier (vanilla skills 0.5x,
   death drain 0; fallback Honja/ExperienceTuner), OdinPlus/PotionPlus 4.3.4
   (Herblore), optional Smoothbrain/Exploration, RecentXpBars or BetterUI.
   Accepted gaps: Thieving, Prayer drain, tick combat, minigames.
12. Feel-test `Custom_AttackSpeed` (whip 1.2 / godsword 0.9 / scythe 0.95).
   EpicLoot no-magic-drops still needs an in-game confirm (tests 27-29).
14. OSRS item names stay as-is unless the profile is ever shared publicly.
15. Server: flip KG `Use Marketplace Locally` -> false ONLY once the server
   exists (#1 stack risk); re-run tests 2 and 5-7 there; friend's client needs
   the clone ymls; backups = Gale profile + server world save.
16. `DropOnePerPlayer` per-player-roll semantics: test on the server with a
   temp 100% then 50% entry, both players present (amount-1 side is settled).
17. Server iteration loop = edit -> deploy -> `dropthat:reload` (admin-only).
   Deployment mechanics undecided until the host is picked.
19. Unverified skill names fail OPEN: `Unarmed`, `Crossbows`, `Fishing`,
   `ElementalMagic`, `BloodMagic` (+ three `?? UNVERIFIED` staff lines) — test 10.
20. Superiors (p9-10) untested — tests 5-7. Risk: CLLC "Maximum stars = None"
   clamping the forced level; fallback in HANDOFF T4.
21. KG pack untested in game — tests 13-26; NPC placement needs the
   Marketplace Hammer (admin).
22. p9-4 fishing gates blocked: no bait prefab in any on-disk dump.
23. KG unknowns only a launch settles (HANDOFF §5): `OpenUI` menu words, quest
   cooldown semantics, buff value scale, icon/VFX fields, speech schema. All
   files PARSE (zero per-entry log errors); semantics still open.
26. **NEW** Re-run `raiseskill` after the Blacksmithing 1.3.6 update; if it
   still does nothing, grind or `setkey` around it and re-scan DLL MemberRefs.
27. **NEW** Restore `[WorldSpawner.500]` to `SpawnChance = 8` /
   `SpawnInterval = 900` after test 6.

Done and removed this pass: 1-3, 6-7 (first ModTest), 11 (clone drop PASSED
test 2), 18 (217 loaded), 24 (MWL yaml stripped), 25 (coins stack/weight).

## Decisions locked (change deliberately, not casually)
Smoothbrain skill suite, not WackyEpicMMOSystem · XP 0.5x "authentic grind" ·
EpicLoot restricted to clues+bounties, coins-only adventure economy · CLLC
stars off / loot Vanilla (two-star creatures exist ONLY as Spawn That
superiors) · append-only drop IDs >= 100, entries <=100 items · time-normalized
rates (literal = swap file) · prefab names only from on-disk dumps/mod tables
(`reference\verified-prefab-names.json`, enforced by the validator) · test on
ModTest, Gielheim created only after the stack is stable · Gale as manager
(Thunderstore + Hexium; "Deprecated" can mean "moved to Hexium") · two-player
rented dedicated server, server config authoritative (ServerSync), Gale profile
is the authoring copy · friend joins via Gale profile export (KG + Azu are
Hexium-hosted) · loot rates NOT recalibrated for two players · banks accept
ore on purpose (delete the ore lines in `osrsheim_bank.cfg` to reverse).

## History (compressed)
2026-09-19: plan published -> Valheim 1.0 reinstalled, Gale profile built, KG
10.0.1-beta.1 chosen over 9.9.4, Almanac disabled pending 1.0, all configs
authored, ModTest PASSED (skills, gates, loot, no stars, no magic drops, MWL
POIs); night: p9-9 uniques + pets, co-op scope, 217 gates, skillcapes, EpicLoot
zeroed; late night: web-researched KG pack, superiors, coins-only EpicLoot,
CLLC coins, MWL trainers off, server flags, validator -> `HANDOFF-TESTING.md`.
2026-09-20 morning: tests 1-3a passed; Drop That 100-cap + DropOnePerPlayer
amount-1 discovered and fixed; `raiseskill` traced to Blacksmithing 1.3.5's
4-arg `Character.Message` -> 1.3.6 + Ranching 1.1.9 installed, clean launch.
